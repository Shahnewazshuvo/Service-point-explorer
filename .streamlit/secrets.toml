[sheets]
# Only the spreadsheet ID (the long string in the sheet's URL between
# /d/ and /edit) -- this is what stays hidden from anyone viewing the
# deployed app.
spreadsheet_id = "1v_p1g7est7kGhJHRvCelyi5K3BF7ENS4w0xXlRR3kcs"

# The exact tab names to merge (must match the tab names in the sheet,
# case-sensitive). Reference-only tabs like "Instructions" or "Lists"
# should NOT be included here.
tabs = ["Barishal", "Dhaka", "Chattogram", "Khulna", "Mymensingh", "Rajshahi", "Rangpur", "Syhlet", "Bogura"]
